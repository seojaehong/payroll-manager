# 급여관리 시스템 UX 대수술: 코드 정리부터 대시보드 재설계까지

급여관리 시스템의 UX를 Steve Jobs와 Jony Ive의 디자인 원칙으로 분석하고, 대규모 리팩토링을 진행했습니다.

## 문제 발견: 디자인 원칙 위반

### 1. 코드 중복 문제
`/wages`와 `/reports` 페이지가 각각 871줄, 442줄로 거대했고, 사업장 상세 페이지의 탭 내용과 완전히 중복되어 있었습니다.

```
문제:
/wages (871줄) ≒ /businesses/[id] WagesTab (동일 코드)
/reports (442줄) ≒ /businesses/[id] ReportsTab (동일 코드)
```

### 2. UX 일관성 문제
피드백 방식이 파일마다 달랐습니다:
- 어떤 곳은 `alert()`
- 어떤 곳은 `toast.show()`
- 어떤 곳은 `console.log`

### 3. 정보 구조 문제
**"대시보드가 대시보드 역할을 못함"**

앱을 열면 "사업장을 선택하세요"라는 빈 화면이 나옵니다. 대시보드는 전체 현황을 한눈에 보여줘야 하는데, 특정 사업장을 선택해야만 뭔가가 보입니다.

---

## 해결 1: 코드 중복 제거 (-1,211줄)

### ImportTab 패턴 적용

중복 페이지를 리다이렉트 패턴으로 전환:

```typescript
// /wages/page.tsx (871줄 → 51줄)
export default function WagesRedirectPage() {
  const router = useRouter();
  const selectedBusinessId = useStore((state) => state.selectedBusinessId);

  useEffect(() => {
    if (selectedBusinessId) {
      router.replace(`/businesses/${selectedBusinessId}?tab=wages`);
    }
  }, [selectedBusinessId, router]);

  // 미선택 시 안내 화면
  return <SelectBusinessPrompt />;
}
```

### 결과
| 파일 | Before | After | 감소 |
|------|--------|-------|------|
| wages/page.tsx | 871줄 | 51줄 | -94% |
| reports/page.tsx | 442줄 | 51줄 | -88% |
| **총** | 1,313줄 | 102줄 | **-1,211줄** |

---

## 해결 2: alert() → toast 통일

전체 코드베이스에서 `alert()` 사용을 찾아 `toast.show()`로 교체:

```typescript
// Before
alert('저장되었습니다');

// After
toast.show('저장되었습니다', 'success');
```

### 변환 현황
| 파일 | 변환 수 |
|------|---------|
| WagesTab.tsx | 7 |
| ReportsTab.tsx | 8 |
| PayslipTab.tsx | 6 |
| settings/page.tsx | 10 |
| RetirementTab.tsx | 1 |
| RetireModal.tsx | 2 |
| payslip-pdf.ts | 1 (→ throw Error) |
| retirement-pdf.ts | 1 (→ console.log) |
| **총** | **36개** |

---

## 해결 3: 탭 간소화 (6개 → 4개)

사업장 상세 페이지의 탭이 너무 많았습니다:

### Before (6개 탭)
```
[근로자] [급여] [명세서] [퇴직금] [신고서] [Import]
```

### After (4개 탭 + 서브탭)
```
[근로자] [급여 관리] [퇴직금] [신고서]
              ↓
        [📊 급여 이력] [📨 명세서 발송]
```

**핵심 변경:**
- Import 탭 제거 (WagesTab에서 직접 업로드)
- 급여 + 명세서 통합 (서브탭으로)

---

## 진행 중: 대시보드 구조 재설계

### 문제
```
현재:
┌─────────────────┐
│ BusinessSelector │ ← 선택 필수
│   (선택하세요)   │
└────────┬────────┘
         ↓
┌─────────────────┐
│ 단일 사업장 대시보드│
│   (빈 화면 가능)  │
└─────────────────┘
```

### 계획
```
개선 후:
┌──────────────────────────────────┐
│ 📊 대시보드 (전체 현황)           │
│                                  │
│ [전체 통계] 3개 사업장 | 45명 재직 │
│                                  │
│ ┌────────┐ ┌────────┐ ┌────────┐│
│ │ A사    │ │ B사    │ │ C사    ││
│ │ 카드   │ │ 카드   │ │ 카드   ││
│ └────────┘ └────────┘ └────────┘│
└───────────────┬──────────────────┘
                │ 클릭
                ↓
┌──────────────────────────────────┐
│ 🏢 사업장 상세 (/businesses/[id]) │
│ [근로자] [급여관리] [퇴직금] [신고]│
└──────────────────────────────────┘
```

### 사이드바 간소화
| Before | After |
|--------|-------|
| 7개 메뉴 | 2개 메뉴 |
| 📊 대시보드 | 📊 대시보드 |
| 👥 근로자 | ❌ |
| 💰 급여 | ❌ |
| 📝 신고서 | ❌ |
| 📧 명세서 | ❌ |
| 📥 Import | ❌ |
| ⚙️ 설정 | ⚙️ 설정 |

> 사업장 컨텍스트 메뉴는 사업장 상세 페이지 탭으로 이동

---

## PDCA 문서

이번 작업은 PDCA 방법론으로 체계적으로 관리했습니다:

### Code Cleanup (완료)
- Plan: `docs/01-plan/features/code-cleanup.plan.md`
- Design: `docs/02-design/features/code-cleanup.design.md`
- Analysis: `docs/03-analysis/code-cleanup.analysis.md` (87%)
- Report: `docs/04-report/code-cleanup.report.md`

### Dashboard Restructure (진행 중)
- Plan: `docs/01-plan/features/dashboard-restructure.plan.md` ✅
- Design: `docs/02-design/features/dashboard-restructure.design.md` ✅
- Do: 구현 예정

---

## 핵심 교훈

1. **중복 코드는 리다이렉트로 해결**
   - 같은 기능이 두 곳에 있으면 한 곳으로 통합

2. **UX 피드백은 통일**
   - alert() 금지, toast 훅 사용 규칙화

3. **대시보드는 전체 현황**
   - 선택 없이 즉시 가치 제공

4. **Steve Jobs 질문**
   > "앱을 열자마자 가장 중요한 정보가 보이는가?"

---

## 다음 단계

1. BusinessCard 컴포넌트 구현
2. 대시보드 페이지 재작성
3. Layout 사이드바 간소화
4. Gap Analysis 및 완료 보고서

---

*2026-02-04 | Claude Code + PDCA Methodology*
